version: v2

modules:
  - path: pb
    name: buf.build/acme/worldstate
  - path: submodules/weather/pb
    name: buf.build/acme/weather

deps:
  - buf.build/googleapis/googleapis

lint:
  use:
    - STANDARD
    - UNARY_RPC

breaking:
  use:
    - PACKAGE
