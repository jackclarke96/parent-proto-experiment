version: v2

modules:
  - path: api
    name: buf.build/acme/worldstate
  - path: submodules/weather/proto
    name: buf.build/acme/weather

deps:
  - buf.build/googleapis/googleapis

lint:
  use:
    - DEFAULT
    - UNARY_RPC

breaking:
  use:
    - PACKAGE
